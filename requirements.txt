# Python 3.12 (the notebook was run on 3.12 in a conda env named "ml-env")
# Install with:  pip install -r requirements.txt
#
# Lower bounds reflect the APIs the notebook actually uses, e.g.
# sklearn.preprocessing.TargetEncoder (>=1.3, "smooth" argument >=1.4)
# and shap.PermutationExplainer / shap.maskers.Independent (>=0.44).

# Core data stack
pandas>=2.1
numpy>=1.26
scipy>=1.11

# Modelling
scikit-learn>=1.4
xgboost>=2.0
category_encoders>=2.6

# Explainability
shap>=0.44
eli5>=0.13

# Plotting
matplotlib>=3.8
seaborn>=0.13

# I/O and persistence
pyarrow>=15.0          # shap_long.parquet export
joblib>=1.3

# Notebook environment
jupyterlab>=4.0
ipykernel>=6.29
ipywidgets>=8.1        # SHAP force plots
